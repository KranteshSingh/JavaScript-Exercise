var name;
console.log(name);
// By default the value of a variable is undefine.
var myName = 'Krantesh';
console.log(myName);

// Rules for declaring variable names
// 1. Cannot be a reserved keyword
// 2. Meaningful & descriptive names for variable
// 3. Cannot start with a number (1name)
// 4. Cannot contain a space or hypen (-)

// Use camelCase for writing variable name
// Variables are case sensitive
// Declare each variable on a single line.

let firstName, lastName; // not a good way to declare variable
